{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"omni-weather-forecast-apis","text":"<p>Async Python library that fans out forecast requests across multiple weather providers and normalizes the results into one typed Pydantic schema. It preserves provider-native cadence and time boundaries while converting units and condition codes into a common representation.</p>","path":["omni-weather-forecast-apis"],"tags":[]},{"location":"#features","level":2,"title":"Features","text":"<ul> <li>Multi-provider fan-out with async orchestration and partial-failure tolerance</li> <li>Typed normalized schema — common Pydantic models for minutely, hourly, daily, and alert data</li> <li>Plugin architecture — 13 providers with typed per-provider config validation</li> <li>Resilient by default — retries with exponential backoff (honoring <code>Retry-After</code>), conditional-request HTTP caching, connection pooling limits</li> <li>Rate limiting and quotas — global concurrency and RPS limits with per-provider overrides, plus per-provider daily quota caps</li> <li>Secrets from the environment — reference API keys as <code>${ENV_VAR}</code> placeholders instead of embedding them in config files</li> <li>CLI — loads a TOML config, queries providers, prints a table or JSON, and optionally persists normalized output to SQLite</li> <li>Extensible — response hooks and a documented SQLite schema for downstream ensemble and verification projects</li> </ul>","path":["omni-weather-forecast-apis"],"tags":[]},{"location":"#how-it-works","level":2,"title":"How it works","text":"<ol> <li>Fan-out — A <code>ForecastRequest</code> is dispatched concurrently to every    enabled provider using async tasks, bounded by configurable concurrency    and rate limits. Transient failures (network errors, timeouts, HTTP 429)    are retried with exponential backoff.</li> <li>Normalize — Each provider plugin converts its native response into the    common <code>SourceForecast</code> schema, translating units (e.g. Fahrenheit to    Celsius, mph to m/s) and mapping provider-specific condition codes to a    shared <code>WeatherCondition</code> enum.</li> <li>Aggregate — Results are collected into a single <code>ForecastResponse</code>.    Providers that succeed return <code>ProviderSuccess</code> with their forecasts;    providers that fail return <code>ProviderError</code> with a typed error code. The    response always completes, even if some providers fail.</li> </ol>","path":["omni-weather-forecast-apis"],"tags":[]},{"location":"#where-to-go-next","level":2,"title":"Where to go next","text":"<ul> <li>Getting Started — install and run your first forecast</li> <li>Configuration — every knob in the TOML config</li> <li>Providers — supported providers and their config keys</li> <li>CLI — flags, output formats, and SQLite persistence</li> <li>Extending — plugins, response hooks, and building ensemble/verification tools on top</li> <li>API Reference — generated from the source</li> </ul>","path":["omni-weather-forecast-apis"],"tags":[]},{"location":"api-reference/","level":1,"title":"API Reference","text":"","path":["API Reference"],"tags":[]},{"location":"api-reference/#client","level":2,"title":"Client","text":"<p>Main async aggregation client.</p> <p>Create and initialize an OmniWeather client.</p> <p><code>plugins</code> scopes the plugin set to this client instance — pass a sequence of plugins or a mapping keyed by provider id. When omitted, the client falls back to the process-global registry.</p> <p><code>metrics_hooks</code> receive a MetricEvent for every request attempt, retry, cache hit/miss, and quota consumption.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.client.OmniWeatherClient.initialize","level":2,"title":"initialize  <small><code>async</code></small>","text":"<pre><code>initialize() -&gt; None\n</code></pre> <p>Validate provider configs and initialize provider instances.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.client.OmniWeatherClient.close","level":2,"title":"close  <small><code>async</code></small>","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Close HTTP resources.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.client.OmniWeatherClient.forecast","level":2,"title":"forecast  <small><code>async</code></small>","text":"<pre><code>forecast(request: ForecastRequest) -&gt; ForecastResponse\n</code></pre> <p>Fetch forecasts from the selected providers.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.client.OmniWeatherClient.get_provider_capabilities","level":2,"title":"get_provider_capabilities","text":"<pre><code>get_provider_capabilities() -&gt; dict[\n    ProviderId, PluginCapabilities\n]\n</code></pre> <p>Return capabilities for initialized providers.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.client.OmniWeatherClient.get_configured_providers","level":2,"title":"get_configured_providers","text":"<pre><code>get_configured_providers() -&gt; list[ProviderId]\n</code></pre> <p>Return enabled configured providers.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#request-and-response-types","level":2,"title":"Request and response types","text":"","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.schema.ForecastRequest","level":2,"title":"ForecastRequest","text":"<p>               Bases: <code>BaseModel</code></p> <p>Public forecast request.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.schema.ForecastResponse","level":2,"title":"ForecastResponse","text":"<p>               Bases: <code>BaseModel</code></p> <p>Public aggregated response.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.schema.ForecastResponseSummary","level":2,"title":"ForecastResponseSummary","text":"<p>               Bases: <code>BaseModel</code></p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.schema.ProviderSuccess","level":2,"title":"ProviderSuccess","text":"<p>               Bases: <code>BaseModel</code></p> <p>Successful provider response.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.schema.ProviderError","level":2,"title":"ProviderError","text":"<p>               Bases: <code>BaseModel</code></p> <p>Failed provider response.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.schema.ProviderErrorDetail","level":2,"title":"ProviderErrorDetail","text":"<p>               Bases: <code>BaseModel</code></p> <p>Structured provider error payload.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.schema.SourceForecast","level":2,"title":"SourceForecast","text":"<p>               Bases: <code>BaseModel</code></p> <p>A complete normalized forecast for one model or provider source.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.schema.WeatherDataPoint","level":2,"title":"WeatherDataPoint","text":"<p>               Bases: <code>BaseModel</code></p> <p>A normalized point-in-time forecast row.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.schema.MinutelyDataPoint","level":2,"title":"MinutelyDataPoint","text":"<p>               Bases: <code>BaseModel</code></p> <p>Normalized minutely precipitation point.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.schema.DailyDataPoint","level":2,"title":"DailyDataPoint","text":"<p>               Bases: <code>BaseModel</code></p> <p>Normalized daily summary row.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.schema.WeatherAlert","level":2,"title":"WeatherAlert","text":"<p>               Bases: <code>BaseModel</code></p> <p>Normalized weather alert.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.schema.WeatherCondition","level":2,"title":"WeatherCondition","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Closed normalized condition vocabulary.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.schema.Granularity","level":2,"title":"Granularity","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.schema.ErrorCode","level":2,"title":"ErrorCode","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.schema.ProviderId","level":2,"title":"ProviderId","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Every supported provider has a stable slug.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.schema.ProviderLogEvent","level":2,"title":"ProviderLogEvent  <small><code>dataclass</code></small>","text":"<p>Structured log event emitted by the client for each provider interaction.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#configuration-types","level":2,"title":"Configuration types","text":"","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.config.RetryPolicy","level":2,"title":"RetryPolicy","text":"<p>               Bases: <code>BaseModel</code></p> <p>Retry policy for transient provider failures.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.config.ProviderRegistration","level":2,"title":"ProviderRegistration","text":"<p>               Bases: <code>BaseModel</code></p> <p>Configuration for one registered provider.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.config.RateLimitConfig","level":2,"title":"RateLimitConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Global rate-limiting policy.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.config.HTTPConfig","level":2,"title":"HTTPConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Shared HTTP client settings.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.config.OmniWeatherConfig","level":2,"title":"OmniWeatherConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Top-level client configuration.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#plugin-protocol","level":2,"title":"Plugin protocol","text":"","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.plugin.ProviderConfigModel","level":2,"title":"ProviderConfigModel","text":"<p>               Bases: <code>BaseModel</code></p> <p>Base model for provider-specific config payloads.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.plugin.PluginCapabilities","level":2,"title":"PluginCapabilities","text":"<p>               Bases: <code>BaseModel</code></p> <p>Describes what a provider supports.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.plugin.PluginFetchParams","level":2,"title":"PluginFetchParams","text":"<p>               Bases: <code>BaseModel</code></p> <p>Request parameters passed to one provider.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.plugin.WeatherPlugin","level":2,"title":"WeatherPlugin","text":"<p>               Bases: <code>Protocol</code></p> <p>Protocol that every provider plugin must implement.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.plugin.WeatherPlugin.id","level":3,"title":"id  <small><code>property</code></small>","text":"<pre><code>id: ProviderId\n</code></pre> <p>Unique provider identifier.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.plugin.WeatherPlugin.name","level":3,"title":"name  <small><code>property</code></small>","text":"<pre><code>name: str\n</code></pre> <p>Human-readable provider name.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.plugin.WeatherPlugin.validate_config","level":3,"title":"validate_config","text":"<pre><code>validate_config(config: dict[str, Any]) -&gt; Any\n</code></pre> <p>Return a validated config object or raise ValidationError.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.plugin.WeatherPlugin.initialize","level":3,"title":"initialize  <small><code>async</code></small>","text":"<pre><code>initialize(config: Any) -&gt; PluginInstance\n</code></pre> <p>Return a configured plugin instance.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.plugin.PluginInstance","level":2,"title":"PluginInstance","text":"<p>               Bases: <code>Protocol</code></p> <p>A configured, ready-to-use provider instance.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.plugin.PluginInstance.provider_id","level":3,"title":"provider_id  <small><code>property</code></small>","text":"<pre><code>provider_id: ProviderId\n</code></pre> <p>Return the provider identifier.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.plugin.PluginInstance.fetch_forecast","level":3,"title":"fetch_forecast  <small><code>async</code></small>","text":"<pre><code>fetch_forecast(\n    params: PluginFetchParams, client: AsyncClient\n) -&gt; PluginFetchResult\n</code></pre> <p>Fetch and normalize forecast data.</p> <p>Implementations must not raise. Errors should be reported through PluginFetchError instead.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.plugin.PluginInstance.get_capabilities","level":3,"title":"get_capabilities","text":"<pre><code>get_capabilities() -&gt; PluginCapabilities\n</code></pre> <p>Return provider capabilities metadata.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#metrics","level":2,"title":"Metrics","text":"<p>Structured metric events emitted by the client.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.metrics.MetricKind","level":2,"title":"MetricKind","text":"<p>               Bases: <code>StrEnum</code></p> <p>Kinds of measurements the client emits.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.types.metrics.MetricEvent","level":2,"title":"MetricEvent  <small><code>dataclass</code></small>","text":"<p>One measurement emitted by the client.</p> <p><code>provider</code> is <code>None</code> for cache events, which are observed at the shared HTTP transport where no per-provider attribution exists; those events carry the request <code>url</code> instead.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#opentelemetry-bridge","level":2,"title":"OpenTelemetry bridge","text":"<p>Optional OpenTelemetry bridge for MetricsHook events.</p> <p>Requires the <code>otel</code> extra::</p> <pre><code>pip install \"omni-weather-forecast-apis[otel]\"\n</code></pre>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.otel.create_otel_metrics_hook","level":2,"title":"create_otel_metrics_hook","text":"<pre><code>create_otel_metrics_hook(\n    meter_provider: Any | None = None,\n) -&gt; MetricsHook\n</code></pre> <p>Build a MetricsHook that records events as OpenTelemetry metrics.</p> <p>Instruments created (all under the <code>omni_weather</code> prefix):</p> <ul> <li><code>omni_weather.requests</code> (counter; attrs: provider, outcome)</li> <li><code>omni_weather.request.duration_ms</code> (histogram; attrs: provider)</li> <li><code>omni_weather.retries</code> (counter; attrs: provider, error_code)</li> <li><code>omni_weather.cache</code> (counter; attrs: outcome)</li> <li><code>omni_weather.quota.consumed</code> (counter; attrs: provider)</li> <li><code>omni_weather.quota.exhausted</code> (counter; attrs: provider)</li> </ul> <p><code>meter_provider</code> defaults to the globally configured provider.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#quota-tracking","level":2,"title":"Quota tracking","text":"<p>Daily request-quota tracking for providers with per-day call limits.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.quota.QuotaTracker","level":2,"title":"QuotaTracker","text":"<p>               Bases: <code>Protocol</code></p> <p>Tracks how many requests were sent per provider per UTC day.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.quota.QuotaTracker.get_usage","level":3,"title":"get_usage","text":"<pre><code>get_usage(provider: ProviderId, day: date) -&gt; int\n</code></pre> <p>Return the number of requests recorded for a provider on a day.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.quota.QuotaTracker.record_request","level":3,"title":"record_request","text":"<pre><code>record_request(provider: ProviderId, day: date) -&gt; None\n</code></pre> <p>Record one request for a provider on a day.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.quota.QuotaTracker.try_consume","level":3,"title":"try_consume","text":"<pre><code>try_consume(\n    provider: ProviderId, day: date, limit: int\n) -&gt; bool\n</code></pre> <p>Record one request only when the provider has remaining quota.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.quota.InMemoryQuotaTracker","level":2,"title":"InMemoryQuotaTracker","text":"<p>Process-local quota tracker; state is lost when the process exits.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.quota.InMemoryQuotaTracker.try_consume","level":3,"title":"try_consume","text":"<pre><code>try_consume(\n    provider: ProviderId, day: date, limit: int\n) -&gt; bool\n</code></pre> <p>Record one request only when the provider has remaining quota.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.quota.SqliteQuotaTracker","level":2,"title":"SqliteQuotaTracker","text":"<p>Quota tracker persisted in SQLite so limits survive across runs.</p> <p>The CLI reuses the forecast database, giving free-tier daily caps (e.g. OpenWeather's 1,000 calls/day) durable enforcement.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.quota.SqliteQuotaTracker.try_consume","level":3,"title":"try_consume","text":"<pre><code>try_consume(\n    provider: ProviderId, day: date, limit: int\n) -&gt; bool\n</code></pre> <p>Atomically reserve one daily request when quota remains.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#http-caching","level":2,"title":"HTTP caching","text":"<p>HTTP response caching with conditional-request revalidation.</p> <p>Implements a small, standards-aware cache for GET requests as an httpx2 transport wrapper. Fresh responses (<code>Cache-Control: max-age</code> / <code>Expires</code>) are served without a network round-trip; stale responses that carry validators (<code>ETag</code> / <code>Last-Modified</code>) are revalidated with conditional headers and reused on <code>304 Not Modified</code>.</p> <p>MET Norway's terms of service require <code>If-Modified-Since</code> support and the NWS strongly encourages caching, so the cache is enabled by default.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.http_cache.CachingTransport","level":2,"title":"CachingTransport","text":"<p>               Bases: <code>AsyncBaseTransport</code></p> <p>Wrap a transport with an in-memory ETag/Expires-aware GET cache.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#environment-placeholders","level":2,"title":"Environment placeholders","text":"<p>Environment-variable placeholder resolution for provider configs.</p> <p>Keeps secrets such as API keys out of TOML files. Two placeholder forms are supported inside a provider <code>config</code> block:</p> <ul> <li>a whole-string reference: <code>api_key = \"${OPENWEATHER_API_KEY}\"</code></li> <li>an explicit marker table: <code>api_key = { env = \"OPENWEATHER_API_KEY\" }</code></li> </ul> <p>Resolution is recursive through nested tables and arrays. A placeholder that names an unset environment variable raises :class:<code>EnvVarNotSetError</code>, which surfaces as a per-provider initialization error.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.utils.env_config.EnvVarNotSetError","level":2,"title":"EnvVarNotSetError","text":"<p>               Bases: <code>LookupError</code></p> <p>Raised when a config placeholder references an unset variable.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.utils.env_config.resolve_env_placeholders","level":2,"title":"resolve_env_placeholders","text":"<pre><code>resolve_env_placeholders(value: Any) -&gt; Any\n</code></pre> <p>Recursively resolve <code>${VAR}</code> and <code>{env = \"VAR\"}</code> placeholders.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#sqlite-persistence","level":2,"title":"SQLite persistence","text":"","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.sqlite_store.save_forecast_response","level":2,"title":"save_forecast_response","text":"<pre><code>save_forecast_response(\n    database_path: str | Path, response: ForecastResponse\n) -&gt; int\n</code></pre> <p>Persist one normalized forecast response into SQLite.</p>","path":["API Reference"],"tags":[]},{"location":"api-reference/#omni_weather_forecast_apis.sqlite_store.save_provider_logs","level":2,"title":"save_provider_logs","text":"<pre><code>save_provider_logs(\n    database_path: str | Path,\n    events: list[ProviderLogEvent],\n    run_id: int | None = None,\n) -&gt; None\n</code></pre> <p>Persist provider log events into the provider_logs table.</p>","path":["API Reference"],"tags":[]},{"location":"cli/","level":1,"title":"CLI","text":"<pre><code>uv run omni-weather --config ./config.toml --lat 40.7128 --lon -74.0060\n</code></pre>","path":["CLI"],"tags":[]},{"location":"cli/#flags","level":2,"title":"Flags","text":"Flag Description <code>--config PATH</code> TOML config path (default: <code>~/.config/omni_weather_forecast_apis.toml</code>) <code>--lat</code> / <code>--lon</code> Coordinates in decimal degrees (override config) <code>--sqlite PATH</code> SQLite output file (overrides config); persistence is skipped when neither is set <code>--format {table,json,csv,ndjson}</code> Output format (default: <code>table</code>) <code>--provider ID</code> Restrict to one or more configured providers (repeatable) <code>--granularity {minutely,hourly,daily}</code> Granularity to request (repeatable) <code>--language CODE</code> Provider language preference <code>--include-raw</code> Persist raw provider payloads alongside normalized results <code>--timeout-ms N</code> Override the default request timeout <code>--debug</code> Verbose logging to stderr and a log file next to the SQLite database, or <code>./omni-weather.log</code> when <code>--sqlite</code> is omitted <p>The exit code is <code>0</code> when every provider succeeded, <code>1</code> when at least one provider failed, and <code>2</code> for invalid arguments or configuration/load errors. Partial provider failures are visible to schedulers and shell scripts.</p> <p>Rich tables and loguru debug logging require the <code>cli</code> extra (<code>pip install \"omni-weather-forecast-apis[cli]\"</code>). Without it the CLI degrades gracefully: tables render as plain text and <code>--debug</code> uses stdlib logging.</p>","path":["CLI"],"tags":[]},{"location":"cli/#output-formats","level":2,"title":"Output formats","text":"<p><code>--format table</code> (default) renders a per-provider summary table: status, latency, row counts per granularity, and error details for failures.</p> <p><code>--format json</code> prints the entire normalized <code>ForecastResponse</code> as JSON on stdout — ideal for piping into <code>jq</code> or another program:</p> <pre><code>uv run omni-weather --config config.toml --lat 40.7 --lon -74.0 --format json \\\n  | jq '.results[] | {provider, status}'\n</code></pre> <p><code>--format csv</code> and <code>--format ndjson</code> emit one row/line per forecast data point, flattened for data tooling (pandas, DuckDB, <code>jq</code>). Every row starts with <code>provider</code>, <code>model</code>, and <code>granularity</code> (<code>minutely</code> / <code>hourly</code> / <code>daily</code>), followed by the normalized point fields; fields that don't apply to a row's granularity are empty.</p> <ul> <li>csv — a single wide table with a fixed column order derived from the   normalized schema. Weather alerts are omitted (a note is printed to   stderr) and provider errors are summarized on stderr, so stdout stays   machine-parseable.</li> <li>ndjson — one compact JSON object per line, each tagged with a <code>type</code>   of <code>forecast_point</code>, <code>alert</code>, or <code>provider_error</code>:</li> </ul> <pre><code>uv run omni-weather --config config.toml --lat 40.7 --lon -74.0 --format ndjson \\\n  | jq 'select(.type == \"forecast_point\") | {provider, timestamp, temperature}'\n</code></pre>","path":["CLI"],"tags":[]},{"location":"cli/#sqlite-output","level":2,"title":"SQLite output","text":"<p>When <code>--sqlite</code> (or <code>sqlite</code> in the config) is set, the CLI creates a normalized database with these tables:</p> Table Contents <code>forecast_runs</code> Request metadata per invocation <code>provider_results</code> One row per provider outcome (success or error) <code>source_forecasts</code> One row per model/source forecast within a provider <code>minutely_points</code> Precipitation intensity at minute intervals <code>hourly_points</code> Normalized hourly forecast rows <code>daily_points</code> Normalized daily summary rows <code>alerts</code> Weather alerts and warnings <code>provider_logs</code> Per-provider lifecycle log entries per run <code>provider_quota_usage</code> Requests per provider per UTC day (daily quota enforcement) <p>The <code>stacking_features</code> view joins hourly points with their provider, model, run cycle, and forecast horizon — a ready-made feature matrix for downstream ensemble/ML work (see Extending).</p>","path":["CLI"],"tags":[]},{"location":"configuration/","level":1,"title":"Configuration","text":"<p>The client and CLI both use a TOML configuration file that matches <code>OmniWeatherConfig</code>.</p> <pre><code>latitude = 40.7128\nlongitude = -74.0060\nsqlite = \"forecasts.sqlite\"\ngranularity = [\"hourly\", \"daily\"]\nlanguage = \"en\"\ninclude_raw = false\ndebug = false\ndefault_timeout_ms = 10000\n\n[rate_limiting]\nmax_in_flight = 10\nmax_requests_per_second = 20\n\n[retry]\nmax_attempts = 3\ninitial_backoff_ms = 500\nmax_backoff_ms = 8000\nbackoff_multiplier = 2.0\njitter = true\n\n[http]\nmax_connections = 20\nmax_keepalive_connections = 10\nconnect_timeout_ms = 5000\ncache_enabled = true\ncache_max_entries = 256\n\n[[providers]]\nplugin_id = \"open_meteo\"\nenabled = true\nconfig = { models = [\"best_match\", \"ecmwf_ifs025\"] }\n\n[[providers]]\nplugin_id = \"openweather\"\nconfig = { api_key = \"${OPENWEATHER_API_KEY}\" }\nrate_limit_rps = 1.0\ntimeout_ms = 8000\nmax_requests_per_day = 900\n</code></pre>","path":["Configuration"],"tags":[]},{"location":"configuration/#top-level-options","level":2,"title":"Top-level options","text":"Key Default Description <code>providers</code> — List of provider registrations (required) <code>latitude</code> / <code>longitude</code> <code>None</code> Default coordinates for the CLI <code>sqlite</code> <code>None</code> Default SQLite output path for the CLI; persistence is skipped when unset <code>granularity</code> <code>[\"hourly\", \"daily\"]</code> Granularities to request <code>language</code> <code>\"en\"</code> Provider language preference <code>include_raw</code> <code>false</code> Persist raw provider payloads <code>default_timeout_ms</code> <code>10000</code> Per-provider fetch timeout <code>debug</code> <code>false</code> Verbose CLI logging","path":["Configuration"],"tags":[]},{"location":"configuration/#retry-policy-retry","level":2,"title":"Retry policy — <code>[retry]</code>","text":"<p>Transient failures — network errors, timeouts, and HTTP 429 rate limits — are retried with exponential backoff and jitter. A server-provided <code>Retry-After</code> header is honored; retries are abandoned when it exceeds 60 seconds. Non-transient failures such as auth errors are never retried.</p> Key Default Description <code>max_attempts</code> <code>3</code> Total attempts per provider fetch (1 disables retries) <code>initial_backoff_ms</code> <code>500</code> Delay before the first retry <code>max_backoff_ms</code> <code>8000</code> Backoff ceiling <code>backoff_multiplier</code> <code>2.0</code> Exponential growth factor <code>jitter</code> <code>true</code> Randomize each delay to avoid thundering herds <p>Setting only one of <code>initial_backoff_ms</code> / <code>max_backoff_ms</code> adjusts the other's default to keep <code>initial &lt;= max</code>; setting both to conflicting values is a validation error.</p> <p>A per-provider <code>retry</code> table on a registration overrides the global policy.</p>","path":["Configuration"],"tags":[]},{"location":"configuration/#http-client-http","level":2,"title":"HTTP client — <code>[http]</code>","text":"Key Default Description <code>max_connections</code> <code>20</code> Connection pool cap <code>max_keepalive_connections</code> <code>10</code> Idle keep-alive connections <code>connect_timeout_ms</code> <code>5000</code> TCP/TLS connect timeout <code>cache_enabled</code> <code>true</code> Conditional-request response cache <code>cache_max_entries</code> <code>256</code> In-memory cache size <p>Setting only one of <code>max_connections</code> / <code>max_keepalive_connections</code> adjusts the other's default to keep <code>keepalive &lt;= connections</code>; setting both to conflicting values is a validation error.</p> <p>The cache is standards-aware: responses with <code>Cache-Control: max-age</code> or <code>Expires</code> are served from memory while fresh, and stale responses carrying <code>ETag</code>/<code>Last-Modified</code> validators are revalidated with conditional requests and reused on <code>304 Not Modified</code>. Responses that declare <code>Vary</code> are only reused for requests sending the same values for the named headers, and <code>Vary: *</code> responses are never cached. MET Norway's terms of service require this behavior and the NWS strongly encourages it. Requests carrying <code>Authorization</code> or <code>Cookie</code> headers bypass the shared cache.</p>","path":["Configuration"],"tags":[]},{"location":"configuration/#provider-registrations-providers","level":2,"title":"Provider registrations — <code>[[providers]]</code>","text":"Key Default Description <code>plugin_id</code> — Provider slug (see Providers) <code>config</code> — Provider-specific config dict <code>enabled</code> <code>true</code> Toggle without deleting the block <code>rate_limit_rps</code> <code>None</code> Per-provider requests-per-second cap <code>timeout_ms</code> <code>None</code> Per-provider timeout override <code>max_requests_per_day</code> <code>None</code> Daily quota cap (see below) <code>retry</code> <code>None</code> Per-provider retry policy override","path":["Configuration"],"tags":[]},{"location":"configuration/#daily-quotas","level":2,"title":"Daily quotas","text":"<p>Most free tiers are capped per day, not per second. Set <code>max_requests_per_day</code> on a registration and the client refuses to fetch once the day's budget (UTC) is spent, returning a <code>quota_exceeded</code> error for that provider instead of burning the quota:</p> <pre><code>[[providers]]\nplugin_id = \"openweather\"\nconfig = { api_key = \"${OPENWEATHER_API_KEY}\" }\nmax_requests_per_day = 900\n</code></pre> <p>Each fetch attempt (including retries) counts one request. The CLI persists counts in the SQLite database (<code>provider_quota_usage</code> table) so limits survive across runs; library users can pass any <code>omni_weather_forecast_apis.quota.QuotaTracker</code> implementation with atomic <code>try_consume</code> support to the client (<code>InMemoryQuotaTracker</code> is the default, <code>SqliteQuotaTracker</code> is bundled).</p>","path":["Configuration"],"tags":[]},{"location":"configuration/#environment-variable-placeholders","level":2,"title":"Environment variable placeholders","text":"<p>Any string value inside a provider <code>config</code> block can reference an environment variable instead of embedding a secret:</p> <pre><code># whole-string reference\nconfig = { api_key = \"${OPENWEATHER_API_KEY}\" }\n\n# explicit marker table (equivalent)\nconfig = { api_key = { env = \"OPENWEATHER_API_KEY\" } }\n</code></pre> <p>Resolution happens when the client initializes and recurses through nested tables and arrays. A placeholder naming an unset variable turns into a per-provider initialization error; other providers are unaffected. Partial interpolation (<code>\"prefix-${VAR}\"</code>) is intentionally not supported — only whole-string placeholders are resolved.</p>","path":["Configuration"],"tags":[]},{"location":"extending/","level":1,"title":"Extending","text":"<p>This library deliberately stops at collecting and normalizing forecasts. Higher-level products — consensus/ensemble forecasts, forecast verification and provider accuracy scoring — are meant to live in separate packages built on three extension points.</p>","path":["Extending"],"tags":[]},{"location":"extending/#1-response-hooks","level":2,"title":"1. Response hooks","text":"<p>Register callables that receive every completed <code>ForecastResponse</code>. Hooks may be sync or async; failures are logged and never break the forecast.</p> <pre><code>from omni_weather_forecast_apis.client import create_omni_weather\nfrom omni_weather_forecast_apis.types import ForecastResponse\n\n\nasync def record_for_verification(response: ForecastResponse) -&gt; None:\n    \"\"\"e.g. ship normalized forecasts to a verification store.\"\"\"\n    for result in response.results:\n        ...\n\n\nclient = await create_omni_weather(\n    config,\n    response_hooks=[record_for_verification],\n)\ntry:\n    ...\nfinally:\n    await client.close()\n</code></pre> <p>This is the natural seam for an ensemble package (compute a consensus from <code>response.results</code> and publish it) or a verification package (persist each forecast now, compare against observations later).</p>","path":["Extending"],"tags":[]},{"location":"extending/#2-custom-provider-plugins","level":2,"title":"2. Custom provider plugins","text":"<p>Any object satisfying the <code>WeatherPlugin</code> protocol can be used, including from outside this package. The preferred mechanism is per-client injection — it keeps plugin sets isolated between clients and avoids mutating global state:</p> <pre><code>from omni_weather_forecast_apis.client import create_omni_weather\nfrom omni_weather_forecast_apis.plugins import open_meteo_plugin\n\nclient = await create_omni_weather(\n    config,\n    plugins=[my_plugin, open_meteo_plugin],\n)\n</code></pre> <p><code>plugins</code> accepts a sequence of plugins or a mapping keyed by <code>ProviderId</code>. When omitted, the client uses the process-global registry, which the CLI relies on and which remains available for global registration:</p> <pre><code>from omni_weather_forecast_apis.plugins import register_plugin\n\nregister_plugin(my_plugin)\n</code></pre> <p>Subclass <code>BasePlugin</code>/<code>BasePluginInstance</code> from <code>omni_weather_forecast_apis.plugins._base</code> to inherit config validation, HTTP error mapping, and the normalized point builders.</p>","path":["Extending"],"tags":[]},{"location":"extending/#3-the-sqlite-feature-matrix","level":2,"title":"3. The SQLite feature matrix","text":"<p>The CLI's SQLite output is a stable, documented schema (see CLI). The <code>stacking_features</code> view exposes one row per provider/model/hour with the forecast horizon and NWP run cycle — exactly the shape an ensemble stacker or verification job needs:</p> <pre><code>SELECT valid_time, provider, model, horizon_hours, temperature\nFROM stacking_features\nWHERE valid_time_unix BETWEEN :start AND :end\nORDER BY valid_time, provider;\n</code></pre> <p>A verification project can join this view against observed weather by <code>valid_time</code> and location to score each provider's historical accuracy; an ensemble project can use it as a training feature matrix and weight providers accordingly.</p>","path":["Extending"],"tags":[]},{"location":"extending/#metrics-hooks","level":2,"title":"Metrics hooks","text":"<p>Register <code>MetricsHook</code> callables to observe every request attempt, retry, HTTP-cache lookup, and quota consumption as typed <code>MetricEvent</code>s:</p> <pre><code>from omni_weather_forecast_apis import MetricEvent, create_omni_weather\n\ndef to_statsd(event: MetricEvent) -&gt; None:\n    ...\n\nclient = await create_omni_weather(config, metrics_hooks=[to_statsd])\n</code></pre> <p>Hook failures are logged and never break a forecast. An OpenTelemetry bridge ships as the <code>otel</code> extra (<code>omni_weather_forecast_apis.otel.create_otel_metrics_hook</code>).</p>","path":["Extending"],"tags":[]},{"location":"extending/#quota-trackers","level":2,"title":"Quota trackers","text":"<p>The daily-quota mechanism is also pluggable: implement the <code>QuotaTracker</code> protocol (<code>get_usage</code> / <code>record_request</code> / <code>try_consume</code>) to back quota accounting with your own store (Redis, Postgres, ...) and pass it to the client:</p> <pre><code>client = await create_omni_weather(config, quota_tracker=my_tracker)\n</code></pre>","path":["Extending"],"tags":[]},{"location":"getting-started/","level":1,"title":"Getting Started","text":"","path":["Getting Started"],"tags":[]},{"location":"getting-started/#install","level":2,"title":"Install","text":"<pre><code>uv sync\n</code></pre> <p>Requires Python 3.13+.</p>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#minimal-cli-run","level":2,"title":"Minimal CLI run","text":"<p>Open-Meteo and MET Norway require no API keys, so this works out of the box:</p> <pre><code>cat &gt; config.toml &lt;&lt; 'EOF'\n[[providers]]\nplugin_id = \"open_meteo\"\nconfig = { models = [\"best_match\"] }\n\n[[providers]]\nplugin_id = \"met_norway\"\nconfig = { user_agent = \"MyApp/1.0 ops@example.com\" }\nEOF\n\nuv run omni-weather \\\n  --config ./config.toml \\\n  --lat 40.7128 \\\n  --lon -74.0060 \\\n  --sqlite ./forecasts.sqlite\n</code></pre> <p>Add <code>--format json</code> to emit the full normalized response as JSON on stdout, or omit <code>--sqlite</code> to skip persistence entirely.</p>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#library-usage","level":2,"title":"Library usage","text":"<pre><code>import asyncio\n\nfrom omni_weather_forecast_apis.client import create_omni_weather\nfrom omni_weather_forecast_apis.types import (\n    ForecastRequest,\n    OmniWeatherConfig,\n    ProviderId,\n    ProviderRegistration,\n)\n\n\nasync def main() -&gt; None:\n    config = OmniWeatherConfig(\n        providers=[\n            ProviderRegistration(\n                plugin_id=ProviderId.OPEN_METEO,\n                config={\"models\": [\"best_match\"]},\n            ),\n        ],\n    )\n    async with await create_omni_weather(config) as client:\n        response = await client.forecast(\n            ForecastRequest(latitude=40.7128, longitude=-74.0060),\n        )\n    for result in response.results:\n        print(result.provider.value, result.status)\n\n\nasyncio.run(main())\n</code></pre>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#keeping-api-keys-out-of-config-files","level":2,"title":"Keeping API keys out of config files","text":"<p>Reference environment variables from any provider <code>config</code> value:</p> <pre><code>[[providers]]\nplugin_id = \"openweather\"\nconfig = { api_key = \"${OPENWEATHER_API_KEY}\" }\n</code></pre> <p>See Configuration for details.</p>","path":["Getting Started"],"tags":[]},{"location":"providers/","level":1,"title":"Providers","text":"Provider Plugin ID API Key Notes Open-Meteo <code>open_meteo</code> Optional Free tier; multiple forecast models MET Norway <code>met_norway</code> No Requires <code>user_agent</code> identification NWS / NOAA <code>nws</code> No US coverage only; requires <code>user_agent</code> OpenWeather <code>openweather</code> Yes WeatherAPI <code>weatherapi</code> Yes Tomorrow.io <code>tomorrow_io</code> Yes Visual Crossing <code>visual_crossing</code> Yes Weatherbit <code>weatherbit</code> Yes Meteosource <code>meteosource</code> Yes Pirate Weather <code>pirate_weather</code> Yes Dark Sky-compatible API Stormglass <code>stormglass</code> Yes Hourly only; multi-model Weather Unlocked <code>weather_unlocked</code> Yes Requires <code>app_id</code> + <code>app_key</code> Google Weather <code>google_weather</code> Yes Google Maps Platform Weather API","path":["Providers"],"tags":[]},{"location":"providers/#configuration-reference","level":2,"title":"Configuration reference","text":"<p>Each provider accepts a typed config dict. Required fields are marked in bold. Any string value may be an environment variable placeholder.</p> Provider Config Keys <code>open_meteo</code> <code>api_key</code>?, <code>models</code> (default: <code>[\"best_match\"]</code>), <code>extra_hourly_vars</code>?, <code>extra_daily_vars</code>? <code>met_norway</code> <code>user_agent</code>, <code>altitude</code>?, <code>variant</code> (<code>\"compact\"</code> | <code>\"complete\"</code>, default: <code>\"complete\"</code>) <code>nws</code> <code>user_agent</code>, <code>grid_override</code>? (<code>{office, grid_x, grid_y}</code>) <code>openweather</code> <code>api_key</code>, <code>exclude</code>?, <code>units</code> (<code>\"standard\"</code> | <code>\"metric\"</code> | <code>\"imperial\"</code>, default: <code>\"metric\"</code>) <code>weatherapi</code> <code>api_key</code>, <code>days</code> (1-14, default: 7), <code>aqi</code> (default: false), <code>alerts</code> (default: true) <code>tomorrow_io</code> <code>api_key</code>, <code>fields</code>? <code>visual_crossing</code> <code>api_key</code>, <code>include</code> (default: <code>\"hours,days,alerts\"</code>) <code>weatherbit</code> <code>api_key</code>, <code>hours</code> (1-240, default: 48), <code>units</code> (<code>\"M\"</code> | <code>\"S\"</code> | <code>\"I\"</code>, default: <code>\"M\"</code>) <code>meteosource</code> <code>api_key</code>, <code>sections</code> (default: <code>[\"current\", \"hourly\", \"daily\"]</code>) <code>pirate_weather</code> <code>api_key</code>, <code>extend_hourly</code> (default: false), <code>version</code> (<code>\"1\"</code> | <code>\"2\"</code>, default: <code>\"2\"</code>) <code>stormglass</code> <code>api_key</code>, <code>sources</code> (default: <code>[\"sg\"]</code>), <code>params</code> (list of weather variables) <code>weather_unlocked</code> <code>app_id</code>, <code>app_key</code>, <code>lang</code>? <code>google_weather</code> <code>api_key</code>, <code>hours</code> (1-240, default: 48), <code>days</code> (1-10, default: 10)","path":["Providers"],"tags":[]},{"location":"providers/#google-weather","level":2,"title":"Google Weather","text":"<p>The <code>google_weather</code> plugin talks to the Google Maps Platform Weather API. It needs a Google Maps Platform API key with the Weather API enabled.</p> <ul> <li>Hourly forecasts come from <code>forecast/hours:lookup</code> (paginated, up to 240   hours; the <code>hours</code> config key bounds how much is fetched).</li> <li>Daily forecasts come from <code>forecast/days:lookup</code> (up to 10 days) and   aggregate Google's daytime/nighttime part forecasts into the normalized   daily schema.</li> <li>Responses are requested in metric units and converted to the library's   normalized units (km/h wind speeds become m/s, etc.).</li> </ul>","path":["Providers"],"tags":[]}]}